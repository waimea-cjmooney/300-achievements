{#=====================================================
  Things page
  - List of things passed in as 'things' array
  - Form for adding a new thing
=====================================================#}

{% extends 'pages/base.jinja' %}


{% block title %}

    Platinum Tracker - Home

{% endblock %}


{% block content %}

    <h2>Search results for '{{ search }}'.</h2>
    <article>

        <ul>
            {% for game in games %}

                <li>
                    <a href="/game/{{ game.id }}">
                        <strong>{{ game.name }}</strong> {{ game.added_by }}
                        
                        {% if game.header_img %}
                            <img src="{{ game.header_img }}" alt="Header image" class="header-img">
                        {% else %}
                            <img src="/static/images/missing.png" alt="Missing header" class="header-img">
                        {% endif %}
                    </a>
                </li>

            {% else %}

                <p>No search results for {{ search }} found.</p>

            {% endfor %}
        </ul>

    </article>



    

{% endblock %}

